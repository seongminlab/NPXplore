% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npxplore.R
\name{npx_pipeline}
\alias{npx_pipeline}
\title{Run the complete NPXplore pipeline}
\usage{
npx_pipeline(
  data_file,
  meta_file,
  annotation_file = npx_annotation_file(),
  sample_id_col = "SampleID",
  variable = "Condition",
  test = NULL,
  pair_id = NULL,
  covariates = NULL,
  include_warn = TRUE,
  include_fail = FALSE,
  output_dir = "NPXplore_output",
  logFC = 0,
  adj.p = 0.05,
  run_functional_analysis = TRUE,
  enrichment_timeout = Inf,
  cluster_scale_rows = FALSE,
  boxplot_top_n = NULL
)
}
\arguments{
\item{data_file}{NPX data frame already read with \code{\link[OlinkAnalyze:read_npx]{OlinkAnalyze::read_npx()}}.
NPXplore does not read NPX files internally.}

\item{meta_file}{Metadata data frame returned by a user-side \code{read.csv()} call.}

\item{annotation_file}{Optional annotation XLSX.}

\item{sample_id_col}{Metadata column containing sample identifiers. Defaults
to \code{SampleID}.}

\item{variable}{Metadata column used as the grouping variable.}

\item{test}{Analysis method. Defaults to automatic selection from the
number of levels in \code{variable}.}

\item{pair_id}{Optional pairing identifier.}

\item{covariates}{Optional ANOVA covariates.}

\item{include_warn}{If \code{TRUE}, retain WARN samples. Defaults to \code{TRUE}.
If \code{FALSE}, WARN samples are removed.}

\item{include_fail}{If \code{TRUE}, retain samples with final sample-level
\code{FAIL} status. Defaults to \code{FALSE}.}

\item{output_dir}{Directory in which to save the metadata used for analysis.}

\item{logFC}{Minimum absolute Log2 fold-change for DEPs. Defaults to \code{0}.}

\item{adj.p}{Maximum adjusted p-value for DEPs. Defaults to \code{0.05}.}

\item{run_functional_analysis}{If \code{TRUE}, run GO and PPI analysis. Defaults
to \code{TRUE}.}

\item{enrichment_timeout}{Best-effort R elapsed time limit for each complete
GO/PPI stage, including downloads and figure export. Defaults to \code{Inf}
(no stage limit); first-time STRING downloads can take several minutes.
This is not a guaranteed HTTP deadline: external libraries may handle
time-limit errors internally.}

\item{cluster_scale_rows}{If \code{TRUE}, use per-assay z-scores only for ANOVA
clustering. Defaults to \code{FALSE} to retain raw NPX distances. Statistical
testing, fold changes, and raw NPX heatmaps are not transformed.}

\item{boxplot_top_n}{Optional positive integer limiting individual boxplots to
the top DEPs by adjusted p-value. NULL (default) plots all assays. This
changes only boxplot exports, not statistical tests, DEP cutoffs, GO or PPI.}
}
\value{
A structured pipeline result. Files are organized under \code{Results},
\code{QC}, \code{Tables}, and \code{log}, with the two metadata CSV files at the root.
The \code{output_files} list includes diagnostic, execution-log and session paths.
}
\description{
Run the complete NPXplore pipeline
}
