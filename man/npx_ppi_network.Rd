% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{npx_ppi_network}
\alias{npx_ppi_network}
\title{Draw STRING PPI networks for two-group or ANOVA results}
\usage{
npx_ppi_network(
  analysis,
  data = NULL,
  anno = NULL,
  cluster_result = NULL,
  output_dir = ".",
  score_threshold = 700,
  min_proteins = 3,
  width = 10,
  height = 8,
  dpi = 600,
  seed = 123
)
}
\arguments{
\item{analysis}{Result from an NPXplore statistical analysis.}

\item{data}{Prepared NPX data, used for automatic ANOVA clustering.}

\item{anno}{Optional annotation data with \code{OlinkID} and \code{Human_Pathway}.
Adds pathway networks using the same membership as \code{\link[=npx_pathway_heatmaps]{npx_pathway_heatmaps()}}.}

\item{cluster_result}{Optional result from \code{\link[=npx_cluster_assays]{npx_cluster_assays()}}.}

\item{output_dir}{Pipeline output directory.}

\item{score_threshold}{Minimum STRING interaction score.}

\item{min_proteins}{Minimum mapped proteins per network.}

\item{width, height}{Figure dimensions in inches.}

\item{dpi}{PNG resolution.}

\item{seed}{Network layout seed.}
}
\value{
A list containing per-network \code{results}, successful output \code{files},
\code{status}, \code{reason}, and the \code{summary} and \code{mapping} tables with their file paths.
\code{PPI_summary.csv} distinguishes empty or insufficient inputs, mapping shortages,
absent interactions, query errors, validation errors, and output errors.
PDF/PNG/HTML fields are TRUE for saved formats, FALSE for failures, and NA
when unattempted (including unavailable optional HTML packages).
\code{STRING_ID_mapping.csv} retains unmapped inputs; \code{NotQueried} indicates that
STRING mapping was not completed. A network may return \code{Partial} when some
output formats failed while others were saved.
}
\description{
Draw STRING PPI networks for two-group or ANOVA results
}
