% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npxplore.R
\name{npx_cluster_assays}
\alias{npx_cluster_assays}
\title{Cluster differential assays for multi-condition analysis}
\usage{
npx_cluster_assays(
  data,
  analysis,
  nbclust_args = list(index = "silhouette"),
  seed = 123,
  scale_rows = FALSE
)
}
\arguments{
\item{data}{Prepared assay data.}

\item{analysis}{An object returned by \code{\link[=npx_analyze]{npx_analyze()}}.}

\item{nbclust_args}{Arguments passed to \code{\link[NbClust:NbClust]{NbClust::NbClust()}}. Cluster counts
are capped below both the assay count and number of distinct profiles to
allow the package's additional \code{k + 1} calculation. Multiple indices use
majority vote; ties select the smaller cluster count.}

\item{seed}{Random seed used by clustering.}

\item{scale_rows}{If \code{TRUE}, standardize each assay across samples before
clustering. Defaults to \code{FALSE}, using NPX values directly. This affects
clustering only; the returned \code{matrix} retains the imputed NPX values.}
}
\value{
A list containing the NPX \code{matrix}, \code{clustering_matrix}, optional
\code{scaled_matrix}, cluster assignments, and NbClust result. Fewer than four
variable assays or three distinct profiles raises an
\code{npx_insufficient_input} error rather than assigning arbitrary clusters.
}
\description{
Cluster differential assays for multi-condition analysis
}
